# The browser calls the same Vercel origin. Next.js proxies /api to Render.
NEXT_PUBLIC_API_URL=/api

# Replace with the public URL created by Render, without a trailing slash.
API_PROXY_TARGET=https://creative-loop-api.onrender.com
